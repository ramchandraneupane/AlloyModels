<Alloy>
	<Window>
		<View layout="vertical">
			<Label id="title" text="{title}" />
			<ImageView image="{path}" />
			<Label id="description" text="{description}" />
			<Label id="location" text="{location}" />
			<Label id="photographer" text="{photographer}" />
		</View>
	</Window>
</Alloy>